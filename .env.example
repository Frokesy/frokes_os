# PostgreSQL connection string supplied by your database host.
DATABASE_URL=postgresql://user:password@host:5432/frokes_os?sslmode=require

# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Set to true when the deployment sits behind a trusted HTTPS proxy.
AUTH_TRUST_HOST=true
