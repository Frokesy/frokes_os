import { ArrowUpRight, WalletCards } from "lucide-react";

type Tip = { eyebrow: string; title: string; body: string; action: string };
export function MoneyCard({ tip }: { tip: Tip }) { return <section className="card flex flex-col p-5 sm:p-7"><div className="mb-7 flex items-center justify-between"><div className="eyebrow"><WalletCards size={13}/> Money thought</div><span className="text-xs text-white/30">03</span></div><span className="text-xs font-medium text-[#d6b878]">{tip.eyebrow}</span><h2 className="mt-3 max-w-md text-2xl font-medium leading-tight">{tip.title}</h2><p className="mt-4 max-w-lg text-sm leading-7 text-white/50">{tip.body}</p><div className="mt-auto pt-7"><div className="flex items-center justify-between rounded-xl border border-white/[.07] bg-white/[.025] px-4 py-3"><span className="text-xs text-white/45">{tip.action}</span><ArrowUpRight size={15} className="text-[#d6b878]"/></div><p className="mt-3 text-[10px] leading-relaxed text-white/25">For awareness only, not a promise of financial outcomes.</p></div></section> }
