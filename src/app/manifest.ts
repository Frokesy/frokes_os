import type { MetadataRoute } from "next"; export default function manifest(): MetadataRoute.Manifest { return { name: "Frokes OS", short_name: "Frokes", description: "Learn something. Check in. Be intentional.", start_url: "/", display: "standalone", background_color: "#080a0d", theme_color: "#080a0d", icons: [{ src: "/icon.svg", sizes: "any", type: "image/svg+xml", purpose: "maskable" }] }; }
